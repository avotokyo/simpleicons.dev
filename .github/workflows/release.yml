name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: avotokyo/workflows/release@main
    with:
      publish: false
    permissions:
      contents: write
      id-token: write
